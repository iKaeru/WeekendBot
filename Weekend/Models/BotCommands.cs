namespace Weekend.Models
{
	public static class BotCommands
	{
		public static string CommandName;

		// WIP:
		// Class to store command names (from enum) and corresponding logic methods (as delegated)
	}
}
